/* import { blocks } from '../blockchain/block';

describe('Block', () => {
  let data: string, lastBlock: any, block: any;

  beforeEach(() => {
    data = 'foo';
    lastBlock = blocks.genesis();
    block = blocks.mineBlock(lastBlock, data);
  });

  it('should set the data to match the input', () => {
    expect(block.data).toEqual(data);
  });

  it('should set the `lastHash` to match the hash of the last block', () => {
    expect(block.lastHash).toEqual(lastBlock.hash);
  });
});
 */
